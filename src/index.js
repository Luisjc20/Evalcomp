import React from 'react';
import ReactDOM from 'react-dom';
import { EvalCompApp } from './EvalCompApp';

import './styles.css';

//console.log(process.env);

ReactDOM.render(
    <EvalCompApp />,
  document.getElementById('root')
);
